deploy:
  stage: deploy
  before_script:
  - 'command -v ssh-agent >/dev/null || ( apk add --update openssh )' 
  - eval $(ssh-agent -s)
  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
  - - ssh-add -l
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - ssh-keyscan -p $SSH_PORT $VM_IPADDRESS >> ~/.ssh/known_hosts
  - chmod 644 ~/.ssh/known_hosts
  script:
    - echo $SSH_USER
    - scp -P "$SSH_PORT" "$ENV_FILE" "$SSH_USER@$VM_IPADDRESS:/opt/organizational-wiki/ai-scanner/.env"
    - ssh $SSH_USER@$VM_IPADDRESS -p ${SSH_PORT} "cd /opt/organizational-wiki/ai-scanner && git pull origin master && docker compose up --build -d"
  only:
    - main
  tags:
    - linux
    - bina
    - shell
